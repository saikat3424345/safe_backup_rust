echo "# Safe Backup Rust Tool

This is a simple CLI tool written in Rust to back up and delete files safely.

## Usage

Run the program with:

```bash
cargo run -- backup sample.txt
cargo run -- delete data.txt


